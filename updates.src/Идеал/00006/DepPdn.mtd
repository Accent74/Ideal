SELECT SUM(P_JOURNAL.JP_SUM), AG_ID, W_PERIOD
FROM (MTD_DEPENDS LEFT JOIN P_JOURNAL ON MTD_DEPENDS.MTD_ID_LEFT = P_JOURNAL.MTC_ID) LEFT JOIN MTD_CODES ON MTD_CODES.MTD_ID = P_JOURNAL.MTC_ID
WHERE MTD_ID_RIGHT = 5  AND MTD_TYPE = 101
GROUP BY AG_ID, W_PERIOD;
